# -*- mode: python -*-

block_cipher = None


a = Analysis(['main.py', 'insertion_sort.py', 'heap_sort.py', 'merge_sort.py', 'merge_sort_hibrid.py', 'quick_sort.py', 'quick_sort_hibrid.py'],
             pathex=['/home/valmir/Documentos/Estrutura'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='main',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='main')
